Termo utilizado no contexto do JavaScript para se referir a remoção de código morto.
Ele se fundamenta no `Import` e `Export` para detectar se um módulo está exportado ou importando entre os arquivos.
